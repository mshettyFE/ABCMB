# .readthedocs.yaml
version: 2

git:
  clone:
    depth: 1
    branch: main

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt

formats:
  - pdf
  - epub
